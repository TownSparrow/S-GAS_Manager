# S-GAS Manager

Позволяет малым языковым моделям (SLM) с поддержкой генерации с расширенным поиском (RAG) управлять огромными контекстами путем адаптивного свопинга чанков между памятью GPU и CPU. Применяет семантическо-графовую оценку релевантности перед каждым запросом для динамического управления информацией, что позволяет выйти за пределы видеопамяти на потребительском оборудовании.

## Обзор

S-GAS Manager — это исследовательский проект, который объединяет:
- **Адаптивный свопинг**: Динамическое управление памятью между GPU и CPU
- **Семантическая + Графовая оценка релевантности**: Гибридное ранжирование чанков с использованием косинусного сходства и графовых расстояний
- **RAG для малых языковых моделей**: Расширение контекстных окон для SLM на потребительском оборудовании
- **Веб-интерфейс для тестирования**: Интерактивный чат-клиент для удобного тестирования

## Архитектура проекта

```
S-GAS_MANAGER/
├── src/
│   ├── core/                    # Основные алгоритмические модули
│   ├── modules/
│   │   ├── retrieval/           # Логика эмбеддингов и поиска
│   │   ├── graph/               # Построение графа знаний
│   │   └── swap/                # Управление свопингом памяти
│   ├── web/
│   │   ├── api.py               # FastAPI REST endpoints
│   │   └── static/              # Файлы веб-клиента
│   │       ├── index.html       # Основной интерфейс
│   │       ├── styles.css       # Стили интерфейса
│   │       └── script.js        # Клиентская логика
│   ├── config.py                # Управление конфигурацией
│   └── main.py                  # Основной тестовый скрипт
├── configs/
│   ├── models.json              # Конфигурации моделей
│   └── system_params.json       # Системные параметры
├── scripts/
│   ├── install_env.sh           # Установка окружения
│   └── start_vllm_server.sh     # Запуск сервера vLLM
├── other/
│   └── requirements.txt         # Python зависимости
└── data/                        # Хранение данных (создается автоматически)
```

## Как настроить и запустить

### 1. Подготовка окружения и установка зависимостей

**а) Требования:**
- Установленный Python 3.10+ (рекомендуется Python 3.12)
- NVIDIA GPU с поддержкой CUDA (для оптимальной производительности)
- Минимум 16GB оперативной памяти и 12GB видеопамяти

**б) Используйте скрипт установки для создания и настройки виртуального окружения Python:**
```bash
chmod +x ./scripts/install_env.sh
./scripts/install_env.sh
```

Этот скрипт выполнит:
- Создание виртуального окружения (`S-GAS_Manager_env`)
- Автоматическая активация окружения
- Обновление pip и основных инструментов
- Установка всех Python зависимостей из `other/requirements.txt`
- Загрузка русской модели spaCy для извлечения сущностей

**в) Ручная активация окружения при необходимости:**
```bash
source S-GAS_Manager_env/bin/activate
```

### 2. Проверка и настройка конфигурации

Проверьте основной файл конфигурации:
```
configs/system_params.json
```

Ключевые настройки для проверки:
- `vllm.model_name`: Путь или имя модели из HuggingFace
- `vllm.gpu_memory_utilization`: Использование памяти GPU (0.8 = 80%)
- `vllm.max_model_len`: Максимальная длина контекста
- `embeddings.model`: Модель эмбеддингов для семантического анализа

### 3. Подготовка статических файлов для веб-клиента

Убедитесь, что следующие файлы находятся в `src/web/static/`:
- `index.html` (основной веб-интерфейс)
- `styles.css` (стили и анимации)
- `script.js` (клиентская логика чата)

Ожидаемая структура каталога:
```
src/web/static/
 ├── index.html
 ├── styles.css
 └── script.js
```

### 4. Запуск сервера языковой модели (vLLM)

**Вариант А: Использование предоставленного скрипта:**
```bash
chmod +x ./scripts/start_vllm_server.sh
./scripts/start_vllm_server.sh
```

**Вариант Б: Ручной запуск:**
```bash
source S-GAS_Manager_env/bin/activate

# Извлечение значений конфигурации
CONFIG_FILE="configs/system_params.json"
VLLM_MODEL=$(python -c "import json;print(json.load(open('$CONFIG_FILE'))['vllm']['model_name'])")
VLLM_GPU_MEM=$(python -c "import json;print(json.load(open('$CONFIG_FILE'))['vllm']['gpu_memory_utilization'])")
VLLM_MAX_LEN=$(python -c "import json;print(json.load(open('$CONFIG_FILE'))['vllm']['max_model_len'])")

# Запуск сервера vLLM
vllm serve $VLLM_MODEL \
    --gpu-memory-utilization $VLLM_GPU_MEM \
    --max-model-len $VLLM_MAX_LEN \
    --max-num-seqs 32 \
    --host 0.0.0.0 \
    --port 8000
```

> **Важно:** Держите этот сервер запущенным в отдельном терминале. API сервер зависит от него.

### 5. Запуск основного API сервера (FastAPI/Uvicorn)

В другом терминале, из корневой папки проекта:
```bash
source S-GAS_Manager_env/bin/activate
uvicorn src.web.api:app --reload --host 0.0.0.0 --port 8080
```

Это запустит:
- REST API endpoints по адресам `/api/chat`, `/health`
- Статический файловый сервер для веб-клиента
- Автоматическую перезагрузку при изменениях кода (режим разработки)

### 6. Доступ к веб-клиенту

Откройте браузер и перейдите по адресу: **[http://localhost:8080](http://localhost:8080)**

**Возможности веб-клиента:**
- 🎨 Современный интерфейс чата с анимациями
- 📊 Мониторинг статуса в реальном времени (здоровье сервера, информация о модели)
- 🔧 Переключатель RAG (включить/выключить генерацию с расширенным поиском)
- 📈 Статистика сессии (количество сообщений, размерности эмбеддингов)
- 💾 Экспорт истории чата в JSON
- 🧹 Функция очистки чата
- ⌨️ Горячие клавиши (Ctrl+Enter для отправки)

### 7. Тестирование и диагностика

**Индикаторы успешной настройки:**
- Зеленая точка статуса: "Сервер онлайн • vLLM: healthy"
- Имя модели отображается в информационной панели
- Размерности эмбеддингов показаны после первого запроса

**Устранение неполадок:**
- **Красная точка статуса**: Проверьте, что сервер vLLM запущен на порту 8000
- **Нет ответа**: Проверьте логи терминала для обоих серверов
- **JavaScript ошибки**: Откройте консоль браузера (F12) для проблем на стороне клиента
- **API ошибки**: Проверьте логи FastAPI в терминале

**Endpoints для проверки здоровья:**
- Статус API: `GET http://localhost:8080/health`
- Модели vLLM: `GET http://localhost:8000/v1/models`

### 8. Разработка и тестирование

**Запуск базовых тестов компонентов:**
```bash
source S-GAS_Manager_env/bin/activate
python src/main.py
```

Это проверит:
- Загрузку конфигурации
- Генерацию эмбеддингов
- Базовое подключение системы

**Тестирование API с помощью curl:**
```bash
curl -X POST http://localhost:8080/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "Привет, как дела?", "use_rag": true}'
```

### 9. Остановка серверов

1. Нажмите `Ctrl+C` в каждом терминале, где запущены серверы
2. Деактивируйте виртуальное окружение:
```bash
deactivate
```

## Исследовательские цели

Этот проект реализует и тестирует алгоритм S-GAS для:
- Сокращения операций свопинга на 30-50% по сравнению с существующими алгоритмами
- Улучшения релевантности поиска с использованием семантического + графового ранжирования
- Поддержания задержки в пределах 1.1-1.3x от базовой производительности
- Обеспечения контекстных окон до 200K токенов на потребительском оборудовании

## Содействие проекту

Это исследовательский прототип. При проблемах или предложениях:
1. Проверьте логи для деталей ошибок
2. Убедитесь в правильности всех конфигурационных файлов
3. Убедитесь, что оба сервера работают корректно
4. Протестируйте с различными конфигурациями моделей

---

**Готово к тестированию вашего S-GAS Manager! 🚀**

Веб-интерфейс делает простым эксперименты с различными запросами, мониторинг производительности системы и оценку алгоритма адаптивного свопинга в реальном времени.